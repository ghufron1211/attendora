[phases.setup]
nixPkgs = [
  "...",
  "nodejs_20",
  "gd"
]

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "npm ci"
]

[phases.build]
cmds = [
  "npm run build",
  "php artisan optimize",
  "php artisan storage:link || true"
]

[start]
cmd = "php artisan migrate --force && php artisan db:seed --class=AdminSeeder --force && php artisan storage:link || true && php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"